# 模型所需参数：

```
state["game_state"][current_hp]
state["game_state"][max_hp]
state["game_state"][combat_state][player][block]
state["game_state"][combat_state][player][energy]
state["game_state"][combat_state][player][powers]
state["game_state"][potions]..
state["game_state"][combat_state][monsters]..//如果战斗阶段，否则为NONE
state["game_state"][map]..
state["game_state"][relics][name]
state["game_state"][relics][name][conter]
state["game_state"][screen_type]..//如无则为NONE,screen_type:{"SHOP_SCREEN","REST","MAP","HAND_SELECT","EVENT","CHEST","COMBAT_REWARD","CARD_REWARD","BOSS_REWARD","GRID"}
state["game_state"][screen_state]
state["game_state"][screen_type][deck]
state["game_state"][screen_type][floor]
state["game_state"][screen_type][gold]
state["game_state"][screen_type][ascension_level]
state["game_state"][combat_state][hand]..
state["game_state"][deck]..
```

..表示为列表，无..则为具体信息

game_state详细解析：

###1. 游戏基础信息

```json
"available_commands": ["play", "end", "key", "click", "wait", "state"],
"ready_for_command": true,
"in_game": true
```

- `available_commands`：当前可以执行的游戏指令，如 `play`（打出牌）、`end`（结束回合）、`key`（按键输入）等。
- `ready_for_command`：是否可以执行新指令（当前为 `true`）。
- `in_game`：是否处于游戏中（`true` 代表正在游戏）。

------

### 2. 游戏状态 `game_state`

#### **2.1 屏幕状态**

```json
"screen_type": "NONE",
"screen_state": {}
```

- `screen_type`: `NONE` 代表当前没有特殊的 UI 界面弹出，意味着可以进行普通游戏操作。

------

#### **2.2 战斗状态 `combat_state`**

```json
"combat_state": {
    "draw_pile": [...],
    "discard_pile": [],
    "exhaust_pile": [],
    "cards_discarded_this_turn": 0,
    "times_damaged": 0,
    "monsters": [...],
    "turn": 1,
    "limbo": [],
    "hand": [...],
    "player": {...}
}
```

- **`draw_pile`**：牌库中的卡牌列表，包含攻击牌 (`Strike`)、防御牌 (`Defend`)、负面卡牌 (`Ascender’s Bane`)。

- **`discard_pile`**：弃牌堆（当前为空）。

- **`exhaust_pile`**：已消耗的卡牌堆（当前为空）。

- **`cards_discarded_this_turn`**：本回合丢弃的卡牌数量（当前为 0）。

- **`times_damaged`**：本回合受到的伤害（当前为 0）。

- `monsters`

  ：

  - 目前对战的怪物是 `Jaw Worm`（咬牙虫）。
  - 它的当前血量是 `1/46`，即几乎要被击杀。
  - 它的攻击意图是 `DEBUG`（可能是调试模式）。
  - 预计下一次攻击会造成 `12` 伤害，但 `move_adjusted_damage` 设为 `-1`，可能表示不会实际造成伤害。

- **`turn`**：当前战斗回合数（目前是 `1`）。

- `hand`

  ：

  - 玩家手中有 

    ```
    5
    ```

     张卡牌：

    - **Strike (攻击) x3**
    - **Defend (防御) x2**
    - **Bash (攻击，费用 2) x1**

- `player`

  ：

  - 当前生命值 `68/75`。
  - 能量值 `3`（表示本回合可使用的点数）。
  - `block` 值为 `0`（即没有护盾）。
  - `orbs` 为空，意味着当前职业不是“守护者”。

------

#### **2.3 牌库 `deck`**

```json
"deck": [
    {"name": "Ascender’s Bane", "type": "CURSE", "cost": -2},
    {"name": "Strike", "type": "ATTACK", "cost": 1},
    {"name": "Defend", "type": "SKILL", "cost": 1},
    {"name": "Bash", "type": "ATTACK", "cost": 2}
]
```

- `Ascender’s Bane` 是一个负面卡牌（`CURSE`）。
- 其余卡牌是基本攻击 `Strike`、防御 `Defend` 和 `Bash`。

------

#### **2.4 遗物 `relics`**

```json
"relics": [
    {"name": "Burning Blood", "id": "Burning Blood", "counter": -1},
    {"name": "Neow's Lament", "id": "NeowsBlessing", "counter": 2}
]
```

- `Burning Blood`（燃烧之血）：战斗结束后恢复 `6` 点生命。
- `Neow's Lament`（尼欧的馈赠）：通常会在前几场战斗提供加成（例如怪物战斗直接获胜）。

------

#### **2.5 其他游戏状态**

```json
"gold": 99,
"action_phase": "WAITING_ON_USER",
"act": 1,
"floor": 1,
"ascension_level": 20,
"class": "IRONCLAD"
```

- **`gold`**：当前金币 `99`。
- **`action_phase`**：等待玩家输入（`WAITING_ON_USER`）。
- **`act`**：当前章节（`Act 1`）。
- **`floor`**：当前层数（`1` 层）。
- **`ascension_level`**：20 级（代表最高难度）。
- **`class`**：角色职业为 `IRONCLAD`（铁甲战士）。

### 3. 地图信息 `map`

```json
"map": [
    {"symbol": "M", "children": [{"x": 0, "y": 1}], "x": 1, "y": 0},
    {"symbol": "M", "children": [{"x": 2, "y": 1}], "x": 2, "y": 0},
    ...
]
```

- ```
  symbol
  ```

   代表地图节点：

  - **`M`（怪物战斗）**
  - **`?`（事件房间）**
  - **`$`（商店）**
  - **`E`（精英怪）**
  - **`R`（篝火）**
  - **`T`（宝箱）**

- 地图数据表示了不同路径的分支和连接情况。

###  4. 结论

1. **战斗情况**

   - 玩家手上有 **3 张攻击牌**（Strike, Bash），**2 张防御牌**（Defend）。
   - 当前怪物 `Jaw Worm` 只剩 `1` 点血，意味着下一步可能是直接击杀。
   - 由于 `Jaw Worm` 的 `move_adjusted_damage` 为 `-1`，它可能无法攻击。

2. **整体状态**

   - 玩家拥有 **99 金币**，没有护盾。
   - 遗物包括 **燃烧之血** 和 **尼欧的馈赠**。
   - 当前是 **20 级挑战模式**，难度较高。
   - 地图显示战斗后可以前往事件、商店或篝火等房间。

3. **下一步行动**

   - **最佳行动**：使用 `Strike`（打出 1 点能量的攻击牌），直接击杀 `Jaw Worm`。

   - 其他选项

     ：

     - `Defend` 没有意义，因为怪物的攻击可能不会生效。
     - `Bash`（2 能量）伤害更高，但当前不需要。

#模型输出信息：

- CommunicationMod then waits for a message back from the external process, containing a command to be executed. Possible commands are:
  - START PlayerClass [AscensionLevel] [Seed]
    - Starts a new game with the selected class, on the selected Ascension level (default 0), with the selected seed (random seed if omitted).
    - Seeds are alphanumeric, as displayed in game.
    - This and all commands are case insensitive.
    - Only currently available in the main menu of the game.
  - POTION Use|Discard PotionSlot [TargetIndex]
    - Uses or discards the potion in the selected slot, on the selected target, if necessary.
    - TargetIndex is the index of the target monster in the game's monster array (0-indexed).
    - Only available when potions can be used or discarded.
  - PLAY CardIndex [TargetIndex]
    - Plays the selected card in your hand, with the selected target, if necessary.
    - Only available when cards can be played in combat.
    - Currently, CardIndex is 1-indexed to match up with the card numbers in game.
  - END
    - Ends your turn.
    - Only available when the end turn button is available, in combat.
  - CHOOSE ChoiceIndex|ChoiceName
    - Makes a choice relevant to the current screen.
    - A list of names for each choice is provided in the game state. If provided with a name, the first choice index with the matching name is selected.
    - Generally, available at any point when PLAY is not available.
  - PROCEED
    - Clicks the button on the right side of the screen, generally causing the game to proceed to a new screen.
    - Equivalent to CONFIRM.
    - Available whenever the proceed or confirm button is present on the right side of the screen.
  - RETURN
    - Clicks the button on the left side of the screen, generally causing you to return to the previous screen.
    - Equivalent to SKIP, CANCEL, and LEAVE.
    - Available whenever the return, cancel, or leave buttons are present on the left side of the screen. Also used for the skip button on card reward screens.
  - KEY Keyname [Timeout]
    - Presses the key corresponding to Keyname
    - Possible keynames are: Confirm, Cancel, Map, Deck, Draw_Pile, Discard_Pile, Exhaust_Pile, End_Turn, Up, Down, Left, Right, Drop_Card, Card_1, Card_2, ..., Card_10
    - The actual keys pressed depend on the corresponding mapping in the game options
    - If no state change is detected after [Timeout] frames (default 100), Communication Mod will then transmit the new state and accept input from the game. This is useful for keypresses that open menus or pick up cards, without affecting the state as detected by Communication Mod.
    - Only available in a run (not the main menus)
  - CLICK Left|Right X Y
    - Clicks the selected mouse button at the specified (X,Y) coordinates
    - (0,0) is the upper left corner of the screen, and (1920,1080) is the lower right corner, regardless of game resolution
    - Will move your cursor to the specified coordindates
    - Timeout works the same as the CLICK command
    - Only available in a run
  - WAIT Timeout
    - Waits for the specified number of frames or until a state change is detected, then transmits the current game state (same behavior as Timeout for the CLICK and KEY commands, but no input is sent to the game)
    - Possibly useful for KEY and CLICK commands which are expected to produce multiple state changes as detected by Communication Mod
    - Only available in a run
  - STATE
    - Causes CommunicationMod to immediately send a JSON representation of the current state to the external process, whether or not the game state is stable.
    - Always available.
- Upon receiving a command, CommunicationMod will execute it, and reply again with a JSON representation of the state of the game, when it is next stable.
- If there was an error in executing the command, CommunicationMod will instead send an error message of the form:

```
{"error":"Error message","ready_for_command":True}
```

## 不同screen_type下的screen_state:

#### (1) `SHOP_SCREEN` (商店界面)

输入 `screen_state` 可能包含：

```
{
    "cards": [
        {"id": "Strike_R", "cost": 1, "price": 50},
        {"id": "Defend_R", "cost": 1, "price": 50}
    ],
    "potions": [
        {"id": "PotionOfStrength", "price": 100}
    ],
    "relics": [
        {"id": "Ornamental Fan", "price": 200, "counter": 1}
    ],
    "purge_cost": 75,  # 移除卡牌的费用
    "purge_available": True  # 是否可以移除卡牌
}
```

#### (2) `REST` (篝火休息)

```
{
    "has_rested": True,  # 是否已经休息
    "rest_options": ["SMITH", "REST"]  # 可执行的操作
}
```

#### (3) `MAP` (地图界面)

```
{
    "first_node_chosen": False,  # 是否已经选择了第一个节点
    "current_node": {"symbol": "?", "x": 3, "y": 2},  # 当前所在的地图节点
    "next_nodes": [
        {"symbol": "E", "x": 4, "y": 3},  # 可能的下一个选择
        {"symbol": "?"}
    ],
    "boss_available": True  # 是否能挑战Boss
}
```

#### (4) `HAND_SELECT` (手牌选择)

```
{
    "max_cards": 2,  # 最多可以选多少张牌
    "can_pick_zero": False,  # 是否可以不选
    "selected": [
        {"id": "Strike_R", "cost": 1, "type": "ATTACK", "rarity": "BASIC"}
    ],
    "hand": [
        {"id": "Defend_R", "cost": 1, "type": "SKILL", "rarity": "BASIC"}
    ]
}
```

#### (5) `EVENT` (事件界面)

```
{
    "event_id": "Fountain of Blood",
    "options": [
        {"choice_index": 0, "disabled": False},
        {"choice_index": 1, "disabled": True}
    ]
}
```

#### (6) `CHEST` (宝箱界面)

```
{
    "chest_open": False  # 是否已经打开
}
```

#### (7) `COMBAT_REWARD` (战斗奖励界面)

```
{
    "rewards": [
        {"reward_type": "GOLD"},
        {"reward_type": "RELIC"}
    ]
}
```

#### (8) `CARD_REWARD` (卡牌奖励界面)

```
{
    "cards": [
        {"name": "Bash", "type": "ATTACK", "rarity": "UNCOMMON", "cost": 2, "exhausts": False, "is_playable": True, "ethereal": False, "upgrades": 1}
    ],
    "bowl_available": False,  # 是否能拿到金色碗
    "skip_available": True  # 是否可以跳过
}
```

#### (9) `BOSS_REWARD` (Boss奖励界面)

```
{
    "relics": [
        {"name": "Snecko Eye", "counter": -1}
    ]
}
```

#### (10) `GRID` (牌库管理界面)

```
{
    "cards": [
        {"id": "Strike_R", "cost": 1, "type": "ATTACK"}
    ],
    "selected_cards": [
        {"id": "Defend_R", "cost": 1, "type": "SKILL"}
    ],
    "for_transform": False,
    "confirm_up": True,
    "any_number": False,
    "for_upgrade": True,
    "num_cards": 2,
    "for_purge": False
}
```

------

## **3. 可能的输入参数汇总**

| **参数名**          | **类型**     | **界面**                 | **说明**                                                     |
| ------------------- | ------------ | ------------------------ | ------------------------------------------------------------ |
| `screen_type`       | `str`        | 所有界面                 | 当前界面类型，如 `"SHOP_SCREEN"`                             |
| `screen_state`      | `dict`       | 所有界面                 | 当前界面的详细信息                                           |
| `cards`             | `list[dict]` | 商店、卡牌奖励、手牌选择 | 卡牌列表，每张卡可能有 `id`, `cost`, `type`, `rarity`, `exhausts`, `is_playable`, `ethereal`, `upgrades` |
| `potions`           | `list[dict]` | 商店                     | 药水列表，包含 `id`, `price`                                 |
| `relics`            | `list[dict]` | 商店、Boss奖励           | 遗物列表，包含 `id`, `price`, `counter`                      |
| `purge_cost`        | `float`      | 商店                     | 牌移除的价格                                                 |
| `has_rested`        | `bool`       | 篝火                     | 是否已休息                                                   |
| `rest_options`      | `list[str]`  | 篝火                     | 可执行的休息操作，例如 `"SMITH"`                             |
| `first_node_chosen` | `bool`       | 地图                     | 是否选择了第一个节点                                         |
| `current_node`      | `dict`       | 地图                     | 当前位置 `{ "symbol": "?", "x": 3, "y": 2 }`                 |
| `next_nodes`        | `list[dict]` | 地图                     | 下一个可选路径                                               |
| `event_id`          | `str`        | 事件                     | 事件名称                                                     |
| `options`           | `list[dict]` | 事件                     | 可选项，如 `{"choice_index": 0, "disabled": False}`          |
| `chest_open`        | `bool`       | 宝箱                     | 是否已打开                                                   |
| `rewards`           | `list[dict]` | 战斗奖励                 | 战斗奖励，如 `{"reward_type": "GOLD"}`                       |
| `bowl_available`    | `bool`       | 卡牌奖励                 | 是否能拿到金色碗                                             |
| `skip_available`    | `bool`       | 卡牌奖励                 | 是否可以跳过                                                 |